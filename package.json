{
  "name": "agent-z-monorepo",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "mcp": "npm run dev -w @repo/mcp"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.0"
  }
}
